# Observability governance

The ACD uses local, structured and privacy-preserving observability. Technical
logs, operational events, persisted audit records, diagnostics and health
checks are distinct contracts. No telemetry is sent externally.

Policies: `logging_policy.md`, `event_catalog.md`, `audit_policy.md`,
`data_redaction_policy.md` and `diagnostics_policy.md`.
